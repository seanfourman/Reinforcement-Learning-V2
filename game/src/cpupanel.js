// DEPRECATED - the CPU (Red) view was merged into the training control panel (panel.js).
// The header's model selector there swaps the Tune sliders' colour (blue <-> red) and
// values, locks only the red learning sliders via the swing-in lock, and turns the tab
// accent red. There is no separate CPU panel or key any more. This module is intentionally
// empty and no longer imported.
export {};
